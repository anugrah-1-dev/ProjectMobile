<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#5DD62C">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="800dp">

        <!-- Background Wave Top -->
        <ImageView
            android:id="@+id/wave_background"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_alignParentTop="true"
            android:contentDescription="Wave background"
            android:scaleType="fitXY"
            android:src="@drawable/gelombang1" />

        <!-- Logo -->
        <ImageView
            android:id="@+id/logo_mitra"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_below="@id/wave_background"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="-80dp"
            android:contentDescription="Logo Mitra"
            android:src="@drawable/logo_mitra" />

        <!-- White Card Container -->
        <LinearLayout
            android:id="@+id/login_card"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/logo_mitra"
            android:layout_marginTop="40dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/rounded_card_background"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingTop="40dp"
            android:paddingEnd="24dp"
            android:paddingBottom="30dp">

            <!-- Title -->
            <TextView
                android:id="@+id/hello_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="24dp"
                android:text="Reset Password"
                android:textColor="#000000"
                android:textSize="28sp"
                android:textStyle="bold" />

            <!-- Mode Switch -->
            <LinearLayout
                android:id="@+id/switch_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="24dp"
                android:background="@drawable/rounded_edittext_bg"
                android:gravity="center_vertical"
                android:minHeight="56dp"
                android:orientation="horizontal"
                android:paddingStart="16dp"
                android:paddingTop="8dp"
                android:paddingEnd="8dp"
                android:paddingBottom="8dp">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="start|center_vertical"
                    android:text="Mode Reset:"
                    android:textColor="#666666"
                    android:textSize="14sp" />

                <Switch
                    android:id="@+id/reset_mode_switch"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checked="true"
                    android:minWidth="48dp"
                    android:minHeight="48dp"
                    android:padding="8dp"
                    android:text="Google"
                    android:textSize="12sp"
                    android:thumbTint="#5DD62C"
                    android:trackTint="#CCCCCC" />
            </LinearLayout>

            <!-- Email/Username Label -->
            <TextView
                android:id="@+id/identifier_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Email:"
                android:textColor="#000000"
                android:textSize="14sp"
                android:textStyle="normal" />

            <!-- Email/Username Input -->
            <EditText
                android:id="@+id/identifier_input"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginBottom="24dp"
                android:background="@drawable/rounded_edittext_bg"
                android:hint="Masukkan email"
                android:inputType="textEmailAddress"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:textColor="#000000"
                android:textColorHint="#999999"
                android:textSize="14sp" />

            <!-- Current Password Label (untuk SQL mode) -->
            <TextView
                android:id="@+id/current_password_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Password Lama:"
                android:textColor="#000000"
                android:textSize="14sp"
                android:textStyle="normal"
                android:visibility="gone" />

            <!-- Current Password Input (untuk SQL mode) -->
            <RelativeLayout
                android:id="@+id/current_password_container"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginBottom="24dp"
                android:background="@drawable/rounded_edittext_bg"
                android:visibility="gone">

                <EditText
                    android:id="@+id/current_password_input"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_toStartOf="@+id/toggle_current_password"
                    android:background="@null"
                    android:hint="Masukkan password lama"
                    android:inputType="textPassword"
                    android:paddingStart="16dp"
                    android:paddingEnd="8dp"
                    android:textColor="#000000"
                    android:textColorHint="#999999"
                    android:textSize="14sp" />

                <ImageButton
                    android:id="@+id/toggle_current_password"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:layout_marginEnd="4dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Toggle password visibility"
                    android:src="@drawable/ic_visibility_off" />
            </RelativeLayout>

            <!-- New Password Label -->
            <TextView
                android:id="@+id/new_password_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Password Baru:"
                android:textColor="#000000"
                android:textSize="14sp"
                android:textStyle="normal" />

            <!-- New Password Input -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginBottom="24dp"
                android:background="@drawable/rounded_edittext_bg">

                <EditText
                    android:id="@+id/new_password_input"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_toStartOf="@+id/toggle_new_password"
                    android:background="@null"
                    android:hint="Masukkan password baru"
                    android:inputType="textPassword"
                    android:paddingStart="16dp"
                    android:paddingEnd="8dp"
                    android:textColor="#000000"
                    android:textColorHint="#999999"
                    android:textSize="14sp" />

                <ImageButton
                    android:id="@+id/toggle_new_password"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:layout_marginEnd="4dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Toggle password visibility"
                    android:src="@drawable/ic_visibility_off" />
            </RelativeLayout>

            <!-- Confirm Password Label -->
            <TextView
                android:id="@+id/confirm_password_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Konfirmasi Password:"
                android:textColor="#000000"
                android:textSize="14sp"
                android:textStyle="normal" />

            <!-- Confirm Password Input -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginBottom="30dp"
                android:background="@drawable/rounded_edittext_bg">

                <EditText
                    android:id="@+id/confirm_password_input"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_toStartOf="@+id/toggle_confirm_password"
                    android:background="@null"
                    android:hint="Konfirmasi password baru"
                    android:inputType="textPassword"
                    android:paddingStart="16dp"
                    android:paddingEnd="8dp"
                    android:textColor="#000000"
                    android:textColorHint="#999999"
                    android:textSize="14sp" />

                <ImageButton
                    android:id="@+id/toggle_confirm_password"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:layout_marginEnd="4dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Toggle password visibility"
                    android:src="@drawable/ic_visibility_off" />
            </RelativeLayout>

            <!-- Reset Password Button -->
            <Button
                android:id="@+id/reset_password_button"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:background="@drawable/rounded_button_bg"
                android:text="Reset Password"
                android:textAllCaps="false"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!-- Info Text -->
            <TextView
                android:id="@+id/info_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="center"
                android:text="Mode Google akan mengirim email reset ke alamat yang Anda masukkan"
                android:textColor="#666666"
                android:textSize="12sp" />

        </LinearLayout>

    </RelativeLayout>

</ScrollView>